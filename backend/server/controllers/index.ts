const project = require('./project');
const issues = require('./issues');
const members = require('./members');
const membersProject = require('./membersProject');
const issuesMembers = require('./issuesMembers');
module.exports = {
  project,
  issues,
  members,
  membersProject,
  issuesMembers
};
